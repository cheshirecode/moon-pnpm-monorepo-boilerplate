{
  "name": "app-astro",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "exports": {
    "./demo": "./src/demo.ts"
  },
  "scripts": {
    "dev": "astro dev --host 0.0.0.0",
    "typecheck": "astro check",
    "build": "astro build",
    "test": "vitest run",
    "coverage": "vitest run --coverage",
    "lint": "oxlint src --ignore-path ../../.gitignore --quiet"
  },
  "dependencies": {
    "@cheshirecode/demo-contract": "workspace:^",
    "astro": "^7.0.4"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.9",
    "@cheshirecode/tsconfig": "workspace:^",
    "@vitest/coverage-v8": "^4.1.9",
    "typescript": "^7.0.2",
    "vitest": "^4.1.9"
  }
}
