---
"@cheshirecode/create-moon-pnpm-monorepo": patch
---

Update generated monorepos to use the newer pnpm/moon/oxlint pins and run single-package coverage through moon instead of rebuilding every workspace package.
